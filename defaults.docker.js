// This is the default config used by the Docker image, provided as a base so that
// these two settings don't need to be manually set as enviroment variable
export default {
  'prefix': ',',

  'blockedUsers': [ ],
};
